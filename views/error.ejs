<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title><%= title || 'Error' %></title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <%- include('partials/top-navbar') %>
  <div class="container">
    <h1 style="color: #c00;">Error</h1>
    <p><%= error && error.message ? error.message : 'Ha ocurrido un error inesperado.' %></p>
    <% if (error && error.stack) { %>
      <pre style="background:#f8d7da;color:#721c24;padding:1em;border-radius:8px;overflow-x:auto;"><%= error.stack %></pre>
    <% } %>
    <a href="/" class="btn btn-back">&larr; Volver al inicio</a>
  </div>
</body>
</html>
